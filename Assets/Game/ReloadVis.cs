using UnityEngine;

public class ReloadVis : MonoBehaviour
{
    public float reloadCooldown = 1;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
